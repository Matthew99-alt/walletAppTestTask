server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/wallet_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: ${HIKARI_MAX_POOL_SIZE:20}
      minimum-idle: ${HIKARI_MIN_IDLE:5}

      # ожидание свободного соединения: лучше быстрый 503 с ретраем
      # на клиенте, чем минутное зависание запроса
      connection-timeout: ${HIKARI_CONNECTION_TIMEOUT:5000}
      initialization-fail-timeout: 60000

      idle-timeout: ${HIKARI_IDLE_TIMEOUT:600000}

      # Postgres ждёт блокировку SELECT ... FOR UPDATE бесконечно,
      # lock_timeout ограничивает ожидание: по истечении клиент получает 409.
      data-source-properties:
        options: -c lock_timeout=${LOCK_TIMEOUT_MS:3000}

  jackson:
    deserialization:
      # неизвестное поле в запросе — ошибка 400, а не тихое игнорирование:
      # защита от перепутанных payload'ов соседних эндпоинтов
      fail-on-unknown-properties: true

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml